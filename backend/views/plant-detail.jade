extends layout

block content
  .container(style="justify-content: left;")
  .container 
    each item in data
      .card(style='width: 500px;')
        .card-body
          h3.card-title #{item.name}
          .card-body
            h5
             b Durchflussmenge:
             |  #{item.currentFlowRate}L
            h5
             b Letzte Gießung:
             |  #{(item.lastOpening )}
            h5
             b Liter pro Tag:
                - item.currentFlowRate = item.currentFlowRate*7;
             |  #{item.currentFlowRate.toFixed(2)}l
            h5
             b Wasserstand:
             |  #{item.currentWaterLevel}%
            canvas#line-chart(width='100%', height='50')
            form(action='/')
              button.btn.btn-dark(type='submit') Go Back

s